function redireciona(link){
    window.location.replace(link);
}

function respondePergunta(){
    /*Essa funcao seria pra, na lista de perguntas, agnt clicar e ja redirecionar
    pra uma tela (pergunta.html) com a pergunta q agnt clicou no botão*/ 
}
